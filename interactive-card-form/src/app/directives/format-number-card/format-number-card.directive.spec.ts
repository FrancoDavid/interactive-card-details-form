import { FormatNumberCardDirective } from './format-number-card.directive';

describe('FormatNumberCardDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatNumberCardDirective();
    expect(directive).toBeTruthy();
  });
});
